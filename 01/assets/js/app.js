const a = '4.2';
const b = 9;

console.log(a, b);
const sum = a + b
const minus = a - b
const multiply = a * b
const split = a / b
const modulo = a % b
const pow = Math.pow(a, b)

if(sum <20 ){
    console.log('Mniejsze')
}
else {
    console.log('Większe')
}