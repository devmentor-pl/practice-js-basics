
/* rozwiązanie z pętlą for */
const x = 4;
for (let i = 0; i <= 10; i++) {
    console.log(`${x} * ${i} = ${x * i}`)
}


/* rozwiązanie z pętlą while  */
let counter = 0;
while (counter <= 10) {
    console.warn(`${x} * ${counter} = ${x * counter}`)
    counter++
}